# 从鑫柏能（区块链相关）

* AR导航小程序中的难点、亮点是？

* 像AR导航的路径或者是AR模型，是你这边绘制的？还是SDK做的？
TODO:AR模型展示在前端的全链条：数据采集、然后上传到AR平台，3D地图构建、地图部署、创建AR应用绑定地图、POI打点、挂载资源模型。SDK中

* Monorepo讲一下

## 大文件上传
slice切割的是什么类型的数据？

你怎么知道需要断点续传？断点续传需要做什么？
刷新后还能获取到上一次传的那个文件？


这个个ID、那个ID，但其实这些我听不懂
你整个文件的一个上传路径、上传方式怎么做的？然后传到哪里？怎么做记录的？怎么去做的？分片之后如果断点了，那么怎么去续传？

，你只要讲你是怎么读的文件，读出来什么类型，然后怎么去做的分片states，然后分多少片？然后你的数据，你这分片信息不包含文件吗？分片信息是存在了服务端，对不对？嗯，他讲分片信息存在服务端，然后我们去每次上传，上传成功之后会去更新服务端的状态，然后下次进来还可以拿到。然后你拿到的是上传到 AWS 的一个那个路径每一片，然后丢到 AWS 上面去

记录文件的唯一标识符、文件上传的Id、总片数、已上传片数、每个分片的状态等信息
